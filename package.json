{
  "name": "graphics-back",
  "version": "1.0.0",
  "description": "",
  "main": "src/app.js",
  "scripts": {
    "dev": "nodemon src/app.js",
    "start": "node src/app.js",
    "build": "rimraf dist && mkdir -p dist && cp -r src package.json package-lock.json dist/ || (if not exist dist mkdir dist && xcopy /E /I src dist\\src && copy package.json dist\\ && copy package-lock.json dist\\)"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.8.4",
    "cors": "^2.8.5",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.9",
    "rimraf": "^5.0.10"
  }
}
